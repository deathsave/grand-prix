Global Config
=============

    Updated 5/23/26 by unrared.

Custom Code
-----------

The class `Configure` contains a `_handle_mode_availability` method which
handles creating a variable based on an environment variable to globally
enable/disable a mode.
